---
interface Props {
	id?: string;
	show?: boolean;
	title?: string;
	message?: string;
	icon?: string;
}

const {
	id = 'maintenance-overlay',
	show = true,
	title = '🛠️ 維護中',
	message = '系統維護中，請稍後再試',
	icon = '🛠️'
} = Astro.props as Props;
---
<div id={id} class="maintenance-overlay" style={`display: ${show ? 'flex' : 'none'};`}>
	<div class="maintenance-content">
		<h2>{title}</h2>
		<p>{message}</p>
	</div>
</div>

<style>
	.maintenance-overlay {
		position: static;
		width: 100%;
		min-height: 360px;
		height: auto;
		background: rgba(0, 0, 0, 0.8);
		display: flex;
		justify-content: center;
		align-items: center;
		flex-shrink: 0;
		box-sizing: border-box;
		border-radius: 10px;
		margin: 0;
	}

	.maintenance-content {
		text-align: center;
		color: white;
		padding: 2rem;
		width: 800px;
		height: 300px;
		max-width: 90%;
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		flex-shrink: 0;
		word-wrap: normal;
		white-space: normal;
		overflow: hidden;
	}

	.maintenance-content h2 {
		font-size: 2rem;
		margin-bottom: 1rem;
		color: #ffd700;
		word-break: keep-all;
		white-space: nowrap;
	}

	.maintenance-content p {
		font-size: 1.2rem;
		margin-bottom: 1.5rem;
		opacity: 0.9;
		word-break: keep-all;
		white-space: normal;
		line-height: 1.5;
	}
</style>
