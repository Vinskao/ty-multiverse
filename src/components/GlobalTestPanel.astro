---
// 全局測試面板組件
import TestPanel from './TestPanel.astro';
---

<!-- 浮動測試按鈕 -->
<div id="global-test-toggle" class="global-test-toggle">
  <button id="test-toggle-btn" class="test-toggle-btn">
    🔧
  </button>
</div>

<!-- 使用獨立的測試面板組件 -->
<TestPanel />

<style>
  /* 浮動測試按鈕 */
  .global-test-toggle {
    position: fixed;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 10000;
  }

  .test-toggle-btn {
    width: 45px;
    height: 45px;
    border-radius: 50%;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border: none;
    color: white;
    font-size: 18px;
    cursor: pointer;
    box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .test-toggle-btn:hover {
    transform: scale(1.1);
    box-shadow: 0 6px 20px rgba(0,0,0,0.3), 0 0 20px rgba(102, 126, 234, 0.6);
    filter: brightness(1.2);
  }

  .test-toggle-btn:active {
    transform: scale(0.95);
  }
</style>

<script>
  class GlobalTestPanel {
    constructor() {
      this.setupEventListeners();
    }

    private setupEventListeners(): void {
      const toggleBtn = document.getElementById('test-toggle-btn');
      if (toggleBtn) {
        toggleBtn.addEventListener('click', () => this.togglePanel());
      }
    }

    private async togglePanel(): Promise<void> {
      console.log('🔧 測試按鈕被點擊');
      
      // 等待測試面板初始化完成
      let attempts = 0;
      const maxAttempts = 10;
      
      while (attempts < maxAttempts) {
        if ((window as any).testPanel && typeof (window as any).testPanel.openPanel === 'function') {
          console.log('✅ 找到測試面板，打開面板');
          (window as any).testPanel.openPanel();
          return;
        }
        
        console.log(`⏳ 等待測試面板初始化... (${attempts + 1}/${maxAttempts})`);
        await new Promise(resolve => setTimeout(resolve, 500));
        attempts++;
      }
      
      console.error('❌ 測試面板初始化超時');
      alert('測試面板初始化失敗，請刷新頁面重試');
    }
  }

  // 初始化全局測試面板
  new GlobalTestPanel();
</script>
