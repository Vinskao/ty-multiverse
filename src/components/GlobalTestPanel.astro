---
// 全局測試面板組件
import TestPanel from './TestPanel.astro';
---

<!-- 浮動測試按鈕 -->
<div id="global-test-toggle" class="global-test-toggle">
  <button id="test-toggle-btn" class="test-toggle-btn">
    🔧
  </button>
</div>

<!-- 使用獨立的測試面板組件 -->
<TestPanel />

<style>
  /* 浮動測試按鈕 */
  .global-test-toggle {
    position: fixed;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 10000;
  }

  .test-toggle-btn {
    width: 45px;
    height: 45px;
    border-radius: 50%;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border: none;
    color: white;
    font-size: 18px;
    cursor: pointer;
    box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .test-toggle-btn:hover {
    transform: scale(1.1);
    box-shadow: 0 6px 20px rgba(0,0,0,0.3), 0 0 20px rgba(102, 126, 234, 0.6);
    filter: brightness(1.2);
  }

  .test-toggle-btn:active {
    transform: scale(0.95);
  }
</style>

<script>
  class GlobalTestPanel {
    constructor() {
      this.setupEventListeners();
    }

    private setupEventListeners(): void {
      const toggleBtn = document.getElementById('test-toggle-btn');
      if (toggleBtn) {
        toggleBtn.addEventListener('click', () => this.togglePanel());
      }
    }

    private togglePanel(): void {
      // 調用獨立測試面板的 openPanel 方法
      if ((window as any).testPanel) {
        (window as any).testPanel.openPanel();
      } else {
        console.log('測試面板尚未初始化，等待...');
        // 如果測試面板還沒初始化，等待一下再試
        setTimeout(() => {
          if ((window as any).testPanel) {
            (window as any).testPanel.openPanel();
          } else {
            console.error('無法找到測試面板實例');
          }
        }, 1000);
      }
    }
  }

  // 初始化全局測試面板
  new GlobalTestPanel();
</script>
